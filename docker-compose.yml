services:
  mongodb:
    image: mongo:8.0
    container_name: document_insights_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
  redis:
    image: redis:8
    container_name: document_insights_redis
    ports:
      - "6379:6379"

volumes:
  mongo_data: